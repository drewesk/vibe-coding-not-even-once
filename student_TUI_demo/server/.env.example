# SSH Proxy Server Configuration

# Server settings
PORT=3001
HOST=0.0.0.0
NODE_ENV=production

# SSH Key Path (path to private key for VM access)
# This key must be added to /home/student/.ssh/authorized_keys on all VMs
SSH_PRIVATE_KEY_PATH=/root/.ssh/student_vms

# Optional: Logging
LOG_LEVEL=info
